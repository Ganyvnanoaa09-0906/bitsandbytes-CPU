# 「那很有乐子了~」CPU 训练库 — 改动与调用指南

> 目标机器：**i5-10400 / 12GB** 与 **R5-4500U / 16GB**，均 **AVX2 无 NVIDIA 显卡**，
> Windows 10/11。本文覆盖：① 我们（GLM 5.3 + 后续修复）到底改了什么；② 怎么调用；
> ③ 怎么把 CPU 训练速度/内存压到最优（**文本模型 + 生图模型**）。
>
> 硬性约定（来自使用者）：**不开梯度检查点**（逻辑推理/生图训练默认不开，除非上显卡）；
> **不联网拉模型**（本地已验证的模型即可，流量告急）。

---

## 一、改动总览

### 1.1 bitsandbytes（核心库，约 9 个文件改动 + 新增）

| 文件 | 类目 | 说明 |
|---|---|---|
| `csrc/cpu_gdn.cpp` | **新增** | Gated DeltaNet 前向/反向融合内核（AVX2/FMA，OpenMP 按 head 并行，分块检查点反向） |
| `bitsandbytes/gdn_cpu.py` | **新增** | GDN 的 Python 封装 + `patch_transformers`/`patch_fla` 接入 + 环境变量 |
| `csrc/cpu_ops.cpp/.h` | 改 | 8-bit 优化器融合内核 + LUT 加速 + 量化内核（8bit/4bit） |
| `csrc/pythonInterface.cpp` | 改 | 所有 CPU 内核的 C 导出入口（`c*_cpu` 符号） |
| `bitsandbytes/backends/cpu/ops.py` | 改 | CPU 算子的 torch 注册（quantize/dequantize/optimizer） |
| `CMakeLists.txt` | 改 | CPU 后端 + AVX2/FMA + `NOMINMAX` + OpenMP experimental |
| `tests/test_cpu_e2e.py` | 新增 | 端到端验证 |
| `BNB_CPU_TRAINING.md` | 新增 | 构建/验证说明 |
| `build_manual/` | **新增** | 手动 `cl` 直编脚本 + `.def` 导出（绕开 CMake 挂死） |

**实测效果**（i5-10400 本机）：
- GDN 融合内核：fused vs naive **35×**（51ms vs 1813ms），3 dtype 前后向精度达标
- 8-bit 优化器：收敛与 fp32 差 <0.01，状态内存 **3.8x 小**，210 组合 stress 全过
- patch 到真实 Qwen3.5-0.8B：输出偏差 4.4e-4（浮点误差），训练反向正常

### 1.2 我后续修复的问题（GLM 5.3 留下的坑）

| 问题 | 修复 |
|---|---|
| `patch_transformers` 是空操作 | transformers 5.15 的符号是 `torch_recurrent_gated_delta_rule`/`torch_chunk_gated_delta_rule`（不是 `fused_recurrent_gated_delta_rule`），已 patch 全部 3 个符号 |
| scale 语义不匹配 | transformers 慢路径**函数内部**乘 `1/sqrt(K)`，dispatch 层补上；wrapper 改为**先 normalize 后 scale**（否则 L2 归一会抵消缩放）|
| 3 个探针脚本硬依赖 `/proc`、`resource` | 加 Windows psapi 兼容分支（`gdn_train_smoke`/`bench_chunk`/`gdn_mem_probe`/`gdn_layout_probe`）|
| CMake 在本机 configure 挂死 | 提供 `build_manual.bat`（`cl /openmp:experimental` + `.def` 导出）|
| EFST 不支持新式 3D 张量专家 | 见 §2.4 |

### 1.3 pytorch：**没改，也不需要改**

- 仓库里的 `pytorch/` 是浅克隆（0.28GB），**零实质改动**（只有权限位变化）。
- **728 秒反向 bug 的修复在 bitsandbytes 侧**（`gdn_cpu.py` 运行时替换 GDN 慢路径），
  PyTorch 本体不动。
- 官方 wheel `torch 2.13.0+cpu` 已经是 AVX2 + oneDNN 优化。重编源码要拉几 GB + 数小时，
  收益为负（且流量告急）。**结论：继续用官方 wheel**。

---

## 二、怎么调用

### 2.1 启用本地 bitsandbytes

**一键（推荐）**：运行 `deepsleep\setenv.bat`（每次开新终端一次），自动设好
PYTHONPATH / 线程 / 编码，之后所有脚本直接跑。等价于手动：

```bat
:: 方式 A：临时（每次运行前设）
set PYTHONPATH=C:\Users\GanYv\Desktop\deepsleep\那很有乐子了~\bitsandbytes

:: 方式 B：装进环境（一次即可）
py -3.11 -m pip install -e C:\Users\GanYv\Desktop\deepsleep\那很有乐子了~\bitsandbytes
```

> 依赖：`libbitsandbytes_cpu.dll` 必须在 `bitsandbytes\bitsandbytes\` 下（已编译好）；
> 运行需 `VCOMP140.DLL`（装 VS / VC++ 2015-2022 Redist 即有，已确认 System32 存在）。

**import 后立即可用**（`__init__.py` 已挂载 `gdn_cpu`，无需额外 import）：

```python
import bitsandbytes as bnb          # 全部底层加载完成
bnb.optim.AdamW8bit                  # 8bit 优化器
bnb.gdn_cpu.patch_transformers       # GDN 融合内核入口（直接 bnb.gdn_cpu 访问）
from bitsandbytes.gdn_cpu import patch_transformers, fused_recurrent_gated_delta_rule
```

验证（应四行全真）：
```bat
set PYTHONPATH=C:\...\bitsandbytes
py -3.11 -c "import bitsandbytes as bnb; print('AdamW8bit:', hasattr(bnb.optim,'AdamW8bit')); print('gdn_cpu:', hasattr(bnb,'gdn_cpu')); print('lib:', bnb.gdn_cpu.load_native() is not None)"
```
（若 `gdn_cpu: False`，检查 `__init__.py` 末尾是否有 `from . import gdn_cpu`。）

### 2.2 训练脚本标准开头（文本模型）

```python
import os
b = os.environ
b["OMP_NUM_THREADS"] = b["MKL_NUM_THREADS"] = "12"   # i5-10400 逻辑核拉满；R5-4500U 设 6
b["OMP_WAIT_POLICY"] = "active"; b["OMP_DYNAMIC"] = "FALSE"; b["MKL_DYNAMIC"] = "FALSE"

import torch
torch.set_num_threads(12)
try: torch.set_num_interop_threads(1)
except RuntimeError: pass

from transformers import AutoModelForCausalLM
from bitsandbytes.gdn_cpu import patch_transformers
patch_transformers()                 # ← 必须！加载模型前调用，Qwen3-Next/3.5 的 GDN 层走融合内核

# fp32 加载（慢但必须：这台 AVX2 CPU 上 bf16 GEMM 会卡死；降内存靠 8bit 量化冻结权重，见 §3.1）
model = AutoModelForCausalLM.from_pretrained(
    r"C:\Users\GanYv\Desktop\deepsleep\qwen3-1.7B", dtype=torch.float32,
    low_cpu_mem_usage=True, local_files_only=True)
```

### 2.3 bitsandbytes 三个能力

```python
import bitsandbytes as bnb

# ① 8-bit 优化器（CPU 融合内核，状态只给可训练参数）：省内存
opt = bnb.optim.AdamW8bit([p for p in model.parameters() if p.requires_grad], lr=1e-4)

# ② 量化/反量化（权重 8bit/4bit 存储，前向 dequantize 回 fp32 计算）：显卡之外降内存
from bitsandbytes.functional import quantize_blockwise, dequantize_blockwise
q, absmax = quantize_blockwise(w, blocksize=256)          # fp32 -> 8bit 码 + absmax
d = dequantize_blockwise(q, absmax, blocksize=256)        # 8bit -> fp32（CPU 内核）

# ③ Linear8bit/4bit 量化线性层（CUDA 是训练友好，CPU 偏推理/冻结权重）
```

### 2.4 EFST（MoE 专家专项微调，支持新式 3D 张量专家）

```python
from efst import EFSTConfig, apply_efst
info = apply_efst(model, EFSTConfig(
    top_k=2,                 # 校准数据选出最热的 2 个专家/层
    calibration_dataloader=loader,
    tune_router=True,        # 同时微调 router
    lora=True, lora_r=8, lora_alpha=16,
))
print(info.summary())
opt = bnb.optim.AdamW8bit([p for p in model.parameters() if p.requires_grad], lr=1e-4)
```

> 支持 transformers 5.15+ 的 3D 张量专家（`Qwen3NextExperts` 的 `gate_up_proj`/`down_proj`）：
> 自动识别、按专家行解冻、路由统计走 `top_k_index`、拆分后 forward 逐专家循环版。
> 经典 ModuleList 专家走 LoRA；3D 张量专家无法注入 LoRA，自动回退为按行解冻。

### 2.5 低内存工具（lowmem）

```python
from lowmem import DiskTensorStore, DiskCachedDataset
store = DiskTensorStore("./cache_t", mem_cache_bytes=256 << 20)   # 优化器状态落盘 + LRU
store.put("opt_state_0", state); t = store.get("opt_state_0")

ds = DiskCachedDataset(base_ds, cache_dir="./cache_ds", transform=preprocess)  # 数据集落盘
loader = torch.utils.data.DataLoader(ds, batch_size=1, num_workers=2)
```

### 2.6 CPU 环境一键调优（torch_cpu_kit）

```python
import torch_cpu_kit as tck
tck.apply_env(verbose=True)   # 必须在 import torch 之前
import torch
tck.init(verbose=True, model_kind='vision')  # model_kind: 'lm'→8线程, 'vision'/'diffusion'→12线程

# 后台内存/swap 监视器（防 SSD 拷打）：commit 逼近物理内存即 SWAP 标记
ev = tck.start_mem_monitor(interval=30)
# ... 训练循环 ...
tck.suspend_mem_monitor(ev)
```

### 2.7 环境检查 & 编译

```bat
:: 换机器（R5-4500U）编译 CPU DLL：在 VS 的 x64 Native Tools 终端
cd C:\Users\GanYv\Desktop\deepsleep\那很有乐子了~\bitsandbytes
..\build_manual\build_manual.bat

:: 环境检查（电源/页面文件/线程/VC 运行库）
powershell -ExecutionPolicy Bypass -File ..\windows_tune.ps1
```

---

## 三、性能优化配置（文本 + 生图，不开梯度检查点）

### 3.1 通用（两者都吃）

| 手段 | 效果 | 说明 |
|---|---|---|
| **dtype 保持 fp32** ⚠️ | 必需 | **这台 AVX2 CPU 上 bf16 GEMM 是灾难**：无 AVX512-BF16 指令，oneDNN 模拟 bf16 单次 matmul 慢到 >90s（实测卡死）。**f32 训练唯一可行**。 |
| **线程数 = 8** ★ | 最快 | i5-10400 实测：6线程 277、**8线程 313**、12线程 273 GFLOP/s。8 线程最优（cache 利用率好）；12 线程（拉满逻辑核）反而 -13%（超线程争抢）。R5-4500U（6C6T 无超线程）设 6。 |
| **8bit 量化冻结权重** | 权重内存 **-74%** | 用 §2.3 的 quantize/dequantize：冻结层权重存 8bit，前向 dequantize 回 fp32。实测（4096×4096 权张量）：内存 67MB→17MB，速度只慢 **1.21x**（dequantize 9.8ms vs matmul 65.6ms）。**这是 bf16 不可用时的降内存首选**。 |
| **oneDNN + matmul 精度** | 若干 % | `tck.init()` 打开 oneDNN；保持 fp32（`highest`），别开 bf16 降精度。 |
| **页面文件** | 防 SSD 拷打 | 固定大小且放非系统盘（或 SSD 固定不扩容）；见 `windows_tune.ps1`。 |

> **实测基准**（i5-10400，fp32 GEMM 512×2048×8192）：`6线程 277 GFLOP/s · 8线程 313 · 12线程 273`。
> 结论：**开 8 线程，别拉到 12**——超线程帮不到 GEMM 还拖后腿。

### 3.1b 激进编译 & 量化结论（实测，避免走弯路）

| 手段 | 实测 | 结论 |
|---|---|---|
| **重编 DLL 加 `/GL /LTCG /favor:INTEL64 /Qpar`** | 训练段 28s → 23s（**+18%**） | **已固化进 `build_manual.bat`**，换机器照抄 |
| **8bit 全量化基座 + LoRA 训练** | 75s/步（慢 10×） | ❌ 不划算：逐层 dequantize 1.7B + 量化期内存峰值 → swap |
| **bf16 存储 + 前向 `.float()`** | 211s/步（慢 27×） | ❌ 不划算：每步全权重转换 + 6.8GB 临时 → swap |
| fp32 + bnb AdamW8bit | **36.75s/5步（基准）** | ✅ **这是本机最优组合**，别再折腾存储精度 |

> 一句话：**训练内存优化靠「控制激活 + 8bit 优化器 + 页面文件挪 HDD」，不要靠降权重精度存储**
> （8bit/bf16 存储的转换开销在这台 AVX2 CPU 上远大于省内存的收益）。

### 3.2 文本生成模型（LLM 微调）

- **激活内存**随 `max_length × batch` 增长：控制 `max_length`（代码任务 512~768 即可，别 1024+）
  和 `batch_size=1`（CPU 上 batch>1 收益小、内存大涨）。
- **LoRA** 而不是全参：1.7B 全参在 12GB 装不下（fp32 权重 6.8G+梯度 6.8G+状态>20G）；
  LoRA 可训练仅 3.2M 参数，状态极小。
- **混合微调**（代码+角色）见 `jiaoben/train_hybrid_v2.py`。
- **R5-4500U（16GB）**：fp32 1.7B（6.8GB）+ LoRA 不 swap，直接跑；线程 **6**（无超线程）；
  核显为共享内存，当显存无意义。

### 3.3 生图 / 视觉模型（Diffusion U-Net、vision tower）

- **conv 是主角，线程要 12**：实测（i5-10400）2D 卷积吞吐
  `6线程 329 → 8线程 489 → 12线程 618 GFLOP/s`——conv 的 im2col/分块能把超线程吃满，
  **生图训练开 12 线程**（和文本 GEMM 的 8 相反）。`torch_cpu_kit.init(model_kind='vision')` 自动切 12。
- **latent self-attn 用 6**：attention 块（小 GEMM 多）实测 6 线程最快（123ms vs 12线程 147ms）。
  若 attention 占比大（高分辨率注意力），考虑单独降线程，但 conv 主导时整体 12 更优。
- **8bit 量化冻结 conv 权重**：实测 conv 权重 fp32 2.4MB→8bit 0.6MB（**-75%**），前向只慢 **1.04x**
  （dequantize 开销几乎可忽略）——diffusion/VAE 的编码器解码器、冻结 backbone **务必 8bit 量化**。
- **激活是内存大头**（图像 `[B,C,H,W]`），**不开梯度检查点**的前提下：
  - 降分辨率（2² 关系）是第一因子；小 batch（1~2）；
  - **潜变量缓存**：diffusion 训练先把图用 VAE 预编码成潜变量（latent），
    用 `lowmem.DiskCachedDataset` 落盘缓存，避免每步重算 VAE（VAE 一次推理很贵）。
  - 数据预处理（图像增强）吃 CPU：`tck.fast_loader`（num_workers=2 + persistent）就绪。
- **视觉语言模型（vision+LLM）**：vision tower（ViT/CLIP）走 conv+attention，建议 12 线程；
  LLM 部分 GEMM 建议 8——**混合时选 12**（vision 前向占主导或单独 stage），或分阶段训。

> **实测基准**（i5-10400，fp32）：conv 6/8/12线程 = 329/489/618 GFLOP/s；attention 6/8/12 = 123/138/147ms；
> 8bit conv 权重 -75% 内存、前向 +4%。
> **一句话**：生图训练定 `model_kind='vision'`（12线程）+ 8bit 量化冻结层 + 潜变量落盘缓存。

### 3.3b 生图 LoRA 训练实测与量化冻结层（R6, 2026-08-30, R5-4500U 复现）

**统一训练器**：`jiaoben\train_sd_lora.py`（tiny-sd / bk-sdm-tiny / bk-sdm-small 全支持，
`--model` 收模型短名或绝对路径，256px 默认）。配套基准 `jiaoben\bench_sd_train.py`
（`--profiler` 打点、`--quant` 对比量化模式）。

| 模型（UNet 参数） | 256px 单步 | 峰值 RSS | 512px 单步 | 峰值 RSS |
|---|---|---|---|---|
| tiny-sd（323M, 3块） | 2.18s | 3.0GB | — | — |
| bk-sdm-tiny（323M, 3块） | 2.24s | 2.6GB | — | — |
| bk-sdm-small（482M, 4块） | **2.10s** | 3.2GB | 8.29s | 4.1GB |
| bk-sdm-small + `--quant 8bit` | 3.16s | 3.7GB | 12.59s | 4.5GB |

结论：**256px fp32 就是甜点**（500 步 ≈ 18 分钟）；量化冻结层在 256px 是负收益
（慢 1.5x，峰值 RSS 因页滞留还略高），只在 512px / 更大模型 / 内存紧张时使用。

**R6 爆改 bnb 增量（全部纯增量，LLM 侧零回归）**：

1. **新 8bit 融合反量化 GEMM 内核**（`csrc\cpu_ops.cpp` 追加 + `.def` 导出 +
   `bitsandbytes::gemm_8bit` torch op + `functional.fused_dequant_linear_8bit`）：
   `out[M,N] = A[M,K] @ dequant8(B[N,K])^T`，权重全程 uint8（DRAM 流量 1/4）、
   寄存器内解码（线性映射 `code*(2s/255) - s` 用一次 FMA 折叠），
   **无 fp32 权重临时张量**——前向/反向 dx 都可直接复用。数值：与 dequant+GEMM
   相对误差 ~4e-7。
   ⚠️ 必须用**线性 code map**（`code[i]=2i/255-1`）量化权重：bnb 默认的
   `create_dynamic_map()` 是非线性映射（零点加密），无法标量 FMA 解码。
2. **修复 4bit GEMV 在 AVX2 机上的符号错配**：`backends/cpu/ops.py` 回退路径
   原来调用只在 AVX512+BF16 构建下才编译的 `gemv_4bit_inference_cpu_fp4/nf4_bf16`，
   AVX2 机 → AttributeError；且 nf4 传 `data_type=0` 命中 `!=FP4 && !=NF4` → 静默输出全 0。
   现改为所有构建导出的 `cgemv_4bit_inference_cpu_{fp32,bf16,fp16}`（FP4=1/NF4=2），
   修复后 4bit 推理 GEMV 与参考实现逐位一致。
3. **`sd_quant.py`（新）**：`QuantFrozenConv2d`（dequant+conv，backward **重算 dequant**
   不把 fp32 权重挂进计算图）、`QuantFrozenLinear`（8bit 走融合内核，backward 用
   dequant+GEMM 求 dx）、`apply_quant_frozen(module, quant_dtype, exclude_names, cache)`，
   排除名按**首段**匹配（`to_out.0` 与 `to_out` 等价，避免误量 PEFT 目标）。
   NF4 走 `quantize_4bit/dequantize_4bit`（bs=64），8bit 走线性 map（按 K 自动选 bs）。

**回归验证（LLM 侧确认无破坏）**：重编 DLL（`build_manual.bat amd`）后
`py -3.11 -m bitsandbytes.gdn_cpu` → **selftest PASSED**；
`stress_opt.py` → 210 组合 0 失败、determinism diff=0；AdamW8bit 训练步正常。
（构建注意：`cl` 在 GBK 代码页下解析 LF 结尾的中文注释会吞行——**cpp 文件必须 CRLF 行尾**，
或构建加 `/utf-8`。）

### 3.4 性能监视器（训练脚本内自带，防 SSD 遭殃）

`train_hybrid_v2.py` 已内置：后台线程每 20s 打印 `RAM used/commit`，commit 逼近物理内存
即标记 `SWAP!`（说明在拷打 SSD）。也可用 `torch_cpu_kit.mem_report()` 手动打点。

---

## 四、常见问题

| 症状 | 处理 |
|---|---|
| `gdn_cpu native library not found` | DLL 没在包目录 / 没编译；跑 `build_manual.bat` |
| CMake configure 卡死 | 用 `build_manual.bat`（手动 cl，绕开 CMake 的 ABI 探测 bug）|
| 训练中 SSD 疯狂读写 | 内存超载→swap。先 bf16/8bit 减权重内内存，再调页面文件 |
| CPU 打不满 | 线程数（设逻辑核）＋ oneDNN（tck.init）＋ 确认没被 swap 拖累 |
| Qwen3.5 训练 forward 报形状错 | input_ids 必须 2D `[B,T]`（transformers 5.15 对 1D 有 bug），`unsqueeze(0)` |

---

## 五、R5-4500U 实测校准（2026-08-28，本机复现）

目标机器二（**AMD R5-4500U / 6C6T / 16GB / AVX2**，无超线程）实测数据与修复：

### 5.1 线程（确认 6 最优）

| 任务 | 4 线程 | 5 线程 | 6 线程 |
|---|---|---|---|
| 大 GEMM 512×2048×8192 | 101 | 108 | **125** GFLOP/s |
| Conv 256ch 3×3 64×64 | 216 | 216 | **218** GFLOP/s |
| attention 小块 (L=1024) | 253ms | 242ms | **200ms** |
| 小 GEMM 512³ | 133 | **156** | 141 |

**结论**：文本训练一律 **6 线程**（无超线程，物理核=逻辑核=6，直接拉满）。
小 GEMM 单独 5 线程更快（156 vs 141），但 LoRA 训练大 GEMM 主导，默认 6。

> **修复**：`torch_cpu_kit.init(model_kind='lm')` 原按"逻辑核×2/3"算线程（i5 12→8 的
> 公式），在 R5 上会得出 **4 线程**、白丢 20% 吞吐。已改为：无超线程（物理核==逻辑核）
> 直接用物理核，有超线程才 ×2/3。`train_hybrid_v2.py` 同样自动检测（`HY_THREADS` 可覆盖）。

### 5.2 真实训练 5 步对比（Qwen3-1.7B + LoRA，max_len 64，5 条数据）

| 方案 | 耗时/步 | 常驻 RSS | 说明 |
|---|---|---|---|
| **A** fp32 + adafactor | **7.04s** | 7097 MB | 速度基线；峰值 ~11.5GB（16GB 机器偏紧） |
| **B** fp32 + AdamW8bit | 7.21s | 6759 MB | 8bit 优化器（LoRA 状态本就很小时收益有限） |
| **C** 8bit 量化基座 + AdamW8bit | 13.61s | **5109 MB** | 慢 1.93x，但权重常驻省 ~2GB，防 swap |

**与 i5 的差异**：i5 上全量化基座慢 ~10x（375s/5步，见 §3.1b），R5 上只有 ~1.9x——
因为 R5 16GB 内存充足时 fp32 短序列不触发 swap，量化开销是纯 dequantize 成本。
**max_length 拉到 512+ 或系统内存紧张时，A/B 峰值逼近物理内存必 swap，C 是唯一保险**。

### 5.3 推荐配置（R5-4500U）

```
线程       6（torch.set_num_threads(6)，OMP/MKL_NUM_THREADS=6）
dtype      fp32（铁律不变；train_hybrid_v2 已默认 fp32）
max_length 512 以内（HY_MAXLEN 覆盖；1024 会触发 swap）
优化器     adafactor（速度）或 adamw_bnb_8bit（内存，Trainer --opt）
内存告急   8bit 量化冻结层（bench_r5_model.py 方案 C 的实现）
监视       torch_cpu_kit.start_mem_monitor()（本次修复后真正可用）
```

### 5.4 本次修复清单

| 位置 | 问题 | 修复 |
|---|---|---|
| `torch_cpu_kit.py init(lm)` | R5 算出 4 线程（公式套 i5） | 无超线程直接用物理核 |
| `torch_cpu_kit.py` / `gdn_cpu.py` / `train_hybrid_v2.py` | `MEMORYSTATUSEX` 缺 2 字段 → 内存监视器静默失效 | 补全结构 + **psutil 优先**（ctypes 的 GlobalMemoryStatusEx 部分环境返回 INVALID_PARAMETER） |
| `train_hybrid_v2.py` | 旧绝对路径 + 默认 12 线程 + bf16 加载 | 路径相对化 + 自动线程 + `--dtype fp32` 默认 + `--opt` 可选 8bit 优化器 + maxlen 默认 512 |
| `bench_vision.py` / `setenv.bat` | 硬编码 `C:\Users\GanYv\...` | 相对路径（`__file__` / `%~dp0`） |
| `build_manual.bat` | 固定 `/favor:INTEL64`（i5 专用） | **自动检测 CPU 厂商**：AMD → `/favor:AMD64`，Intel → `/favor:INTEL64` |

### 5.5 迁移注意

- 全套代码已可整体拷贝（路径全部相对化）；新机器只需：`setenv.bat` → 编译 DLL（VS 装好后跑
  `build_manual\build_manual.bat`）→ 验证 `py -3.11 -m bitsandbytes.gdn_cpu` 输出 selftest PASSED。
- 基准脚本：`bench_r5_threads.py`（线程校准）/ `bench_r5_model.py`（三方案训练对比）可直接在新机器复跑。

### 5.6 7B 级模型量化训练（4bit，16GB 机器可行）

16GB 机器跑 7B LoRA 的唯一路径：**NF4 量化冻结基座 + 分片流式加载**（fp32 28GB 装不下）。

```bat
:: 转换（分片流式，峰值内存 = 一个分片 ~3.5GB）
py -3.11 convert_quant.py --model D:\<7B> --out D:\deepsleep\models\<7B>-nf4 --dtype nf4

:: 训练（7B 必须 cache_dequant=False：缓存 fp32 权重 24GB 会爆）
py -3.11 -c "from load_quant_model import load_quant_model; m = load_quant_model(r'D:\deepsleep\models\<7B>-nf4', cache_dequant=False)"
```

**1.7B 实测对比（R5-4500U, max_len 64）**：

| 方案 | 速度 | 加载内存 |
|---|---|---|
| fp32 | 7.0s/步 | 6.8GB |
| **nf4 缓存版**（≤3B 模型用） | **4.7s/步** | 3.5GB |
| **nf4 非缓存版**（7B 用） | 7.4s/步 | ~3.5GB |

要点：
- 4bit codebook 是固定常量，`QuantState` 只需 `absmax/blocksize/quant_type/shape`（无需 code）
- 加载器自动冻结 base、只训 LoRA（1.7B 可训练 ~994 万参数）
- 7B 预期 ~25-35s/步（max_len 64）+ ~9GB 峰值；提速靠 max_len 控制 + 部分层缓存
- 工具：`quant_lora.py`（量化层）、`convert_quant.py`（转换）、`load_quant_model.py`（加载）
